def soma(x,y):
    if y-1>0:
        so = x + soma(x,y-1)
        return so
    else:
        return x

numero = float(input("Digite um numero "))
vezes = int(input("Quantas vezes? "))
print(soma(numero,vezes))
